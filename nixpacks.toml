# nixpacks.toml  — full replacement
# ==================================
# Phase 1: download model at build time
[phases.download]
cmds = ["python download_model.py"]

# Phase 2: start FastAPI
[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT}"
